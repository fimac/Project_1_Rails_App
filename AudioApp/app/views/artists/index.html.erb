<h1>Artists index</h1>


<ul>
  <% @all_artists.each do |artist| %>
    <li>
      <%= link_to( artist.name, "/artists/#{artist.id}" ) %>
    </li>
  <% end %>
</ul>

<% if @current_user %>
<p>
<%= link_to("Create Artist Profile", "/artists/new") %>
</p>
<% end %>
